plugins {
    id 'net.neoforged.moddev'
}

repositories {
    mavenLocal()
    maven {
        name = 'Modrinth'
        url = 'https://api.modrinth.com/maven'
        content { includeGroup 'maven.modrinth' }
        metadataSources { artifact() }
    }
}

neoForge {
    version = rootProject.neoforge_version

    runs {
        client { client() }
        server { server() }
    }

    mods {
        factory_blocks {
            sourceSet sourceSets.main
        }
    }
}

dependencies {
    implementation "maven.modrinth:cryonicconfig:neoforge-${rootProject.minecraft_version}-${rootProject.cryonic_version}"
    implementation "maven.modrinth:chisel-reborn:neoforge-${rootProject.minecraft_version}-${rootProject.chisel_version}"
}

// Share the loader-agnostic 'common' source.
sourceSets.main.java {
    srcDir('../common/src/main/java')
}
sourceSets.main.resources.srcDir('../common/src/main/resources')

processResources {
    inputs.property 'version', project.version
    filesMatching('META-INF/neoforge.mods.toml') {
        expand version: project.version
    }
}
