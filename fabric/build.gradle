plugins {
    id 'net.fabricmc.fabric-loom'
}

loom {
    runs {
        client {
            client()
            ideConfigGenerated = true
        }
        server {
            server()
            ideConfigGenerated = true
        }
    }
    mods {
        "factory_blocks" {
            sourceSet sourceSets.main
        }
    }
}

repositories {
    mavenLocal()
    maven {
        name = 'Modrinth'
        url = 'https://api.modrinth.com/maven'
        content { includeGroup 'maven.modrinth' }
        metadataSources { artifact() }
    }
}

dependencies {
    minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
    // 26.1+ is unobfuscated: no 'mappings' line, mods are not remapped, use plain 'implementation'.
    implementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
    implementation "net.fabricmc.fabric-api:fabric-api:${rootProject.fabric_api_version}"
    implementation "maven.modrinth:cryonicconfig:fabric-${rootProject.minecraft_version}-${rootProject.cryonic_version}"
    implementation "maven.modrinth:chisel-reborn:fabric-${rootProject.minecraft_version}-${rootProject.chisel_version}"
}

// Share the loader-agnostic 'common' source.
sourceSets.main.java {
    srcDir('../common/src/main/java')
}
sourceSets.main.resources.srcDir('../common/src/main/resources')

processResources {
    inputs.property 'version', project.version
    filesMatching('fabric.mod.json') {
        expand version: project.version
    }
}
